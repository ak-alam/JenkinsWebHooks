pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'initiating build step'
            }
        }
        stage('Deploy') {
            steps {
                echo 'initiating depoly step'
            }
        }
        stage('Test') {
            steps {
                echo 'initiating Test step'
            }
        }
    }
}
